# Copy this file to `.env` and fill in real values.
#
# Note: This repo ignores `.env*` via `.gitignore`, so we keep this as `env.example`
# (without the leading dot) for reference.

# Database - Use your PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/life_insurance_portal"

# NextAuth - Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key"
NEXTAUTH_URL="http://localhost:3000"

# Resend (Email) - Get your API key from https://resend.com
RESEND_API_KEY="re_xxxxxxxxxx"
# Optional: customize sender (must be a verified domain in production)
# EMAIL_FROM="Emerald Tide Financial <onboarding@resend.dev>"

# Twilio (SMS) - https://www.twilio.com/console
TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
TWILIO_AUTH_TOKEN="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Choose ONE of the following:
# Option A (recommended): Use a dedicated phone number
TWILIO_FROM_NUMBER="+15551234567"
# Back-compat (older env name used in some codebases)
# TWILIO_PHONE_NUMBER="+15551234567"
# Option B: Use a Messaging Service
# TWILIO_MESSAGING_SERVICE_SID="MGxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"


